#include <iostream>

inline void help() {
    printf("Here are the steps to merge point clouds from different views.\n");
    printf("1 - we will need the calibration matrices from different cameras.\n");
    printf("2 - find the board pose in view of different cameras.\n");
    printf("3 - use the common object pose to find the pose relation between cameras.\n");



}

int main(int argc, char** argv) {
    // Load in camera parameters
    return EXIT_SUCCESS;
}